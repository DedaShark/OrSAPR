namespace BottleParameters
{
    /// <summary>
    /// Contains parameter type
    /// </summary>
    public enum ParameterType
    {
        CoverRadius,
        HandleBaseRadius,
        HandleRadius,
        HandleLength,
        Height,
        Width,
        WallThickness,
        IsBottleStraight
    }
}
